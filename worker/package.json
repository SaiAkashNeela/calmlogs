{
  "name": "logging-worker",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "db:setup": "wrangler d1 migrations apply DB --local"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.20230701.0",
    "typescript": "^5.0.0"
  }
}
